{
  "name": "rune-ui",
  "private": true,
  "description": "Rune UI is a headless component library based on Rune",
  "keywords": ["rune-ui", "rune", "headless-component"],
  "license": "MIT",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "devDependencies": {
    "typescript": "^5.8.3"
  },
  "engines": {
    "node": ">=18"
  },
  "packageManager": "pnpm@10.10.0"
}
